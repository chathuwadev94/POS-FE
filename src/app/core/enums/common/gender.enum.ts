export enum Gender {
    MALE = 'm',
    FEMALE = 'f',
    OTHEr = 'o'
}

export enum FormType {
    ADD = "add",
    UPDATE = "edit",
    VIEW = "view"
}

export enum UserStatus {
    PENDING = 0,
    APPROVED = 1,
    HOLD = 2
}